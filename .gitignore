# 依赖
node_modules/

# 编辑器前端的构建产物，由 npm run editor:build 生成
src/editor/ui/dist/

# 使用者自己的内容与交付物：不走 git，复制出去用 SVN 管理
sites/
dist/

# playwright MCP 的截图与会话产物
.playwright-mcp/

# vision router 的截图产物
.dsh-vision-router/

# Reasonix 运行时产物（任务快照等）
.reasonix/

# Reasonix 安装的 skills，不属于本项目的源码
.agents/

# 分享给别人用的打包产物（带 node_modules，几十 MB）
kebab-*.zip
