# kebab 领域语言

把 AI 导出的 HTML 原型、界面截图说明、流程图串成一份有条理、结构完整的导航站点——项目名取"烤串"之意，一枚签子串起一个 module，签上每一块是一个 page。这个上下文里的术语多半来自文档工程，但每个都有本项目自己的边界。

## 术语表

**site（站点）**：
一个产品原型的完整承载单元，对应 `sites/` 下的一个目录。
_避免使用_：作品、项目、工程、产品

**portal（门户页）**：
根 `dist/` 下列出全部 site 的总入口。
_避免使用_：首页、导航页、总览

**module（模块）**：
导航树的一级分组，对应一个功能域。
_避免使用_：分组、分类、章节

**page（页面）**：
导航树上的叶子节点，类型只有 `doc` 和 `proto` 两种。
_避免使用_：条目、节点、section

**doc 页**：
正文是 Markdown 加图片的页面，承载截图说明和流程图。
_避免使用_：说明页、文档页、文字页

**proto 页**：
正文是一个原型目录的页面，不与文字混排。
_避免使用_：原型页、demo 页

**prototype（原型目录）**：
AI 导出的一整套 HTML/CSS/JS 原文件，进库后不再改写。
_避免使用_：原型工程、原型包、原型源码

**page code（页面编号）**：
形如 `LO-01` 的展示编号，由模块前缀加模块内流水构成，分配后锁死。
_避免使用_：页码、页面号、ID

**section number（条目编号）**：
形如 `1.2` 的展示编号，由 doc 页正文的标题层级在渲染时算出。
_避免使用_：小节号、条款号

**doctor（一致性核对）**：
把清单与磁盘之间的失配找出来的核对机制。
_避免使用_：体检、检查、lint

**dangling reference（悬空引用）**：
清单声明的页面，在磁盘上找不到对应物。
_避免使用_：失效引用、断链

**orphan file（孤儿文件）**：
磁盘上的页面文件，没有被清单声明。
_避免使用_：游离文件、多余文件

**import check（导入校验）**：
原型目录进库前做的可运行性核对。
_避免使用_：预检、体检、验货

**stale write（陈旧写入）**：
编辑器内存状态已落后于磁盘却仍要落盘，会覆盖掉别人的改动。
_避免使用_：冲突、覆盖、脏写

## 示例对话

> **我**：我要同时维护两个产品的原型，它们能共用素材吗？
>
> **作者**：不能。一个 site 对应 `sites/` 下的一个目录，素材各自一份，谁也不引用谁。
>
> **我**：我想把登录页的截图说明，和 AI 生成的新登录页放到一起。
>
> **作者**：那是同一 module 下的两个 page——截图说明是 doc 页，AI 生成的是 proto 页。各自拿一个 page code，比如 `LO-01` 和 `LO-02`。
>
> **我**：后来我又往说明里补了两条修改点，编号会变吗？
>
> **作者**：page code 锁死。变的是 doc 页正文里的 section number，那个渲染时才算。
>
> **我**：那我把 `LO-01` 删掉，`LO-03` 会补位成 `LO-01` 吗？
>
> **作者**：不会补位。号只往后发，删中间一页不会让后面的号往上挪。不过删掉模块内末尾那个号之后再新建，会拿回同一个号。
>
> **我**：Agent 改了 `site.json`，我在编辑器里还开着旧状态，直接保存会怎样？
>
> **作者**：那会是一次 stale write，保存会被拦下来，让你先重载。
>
> **我**：AI 导出的原型在同事那边白屏了。
>
> **作者**：import check 本该在它进库时就拦下来——大概率是 module 脚本或者 CDN 外链。
